# React Task

Educational React project.  
The goal is to implement components using **class components**.

## Branches

- `main` — main branch.
- `class-components` — branch with class-based components implementation (pull request is open but not merged).

## Installation and Running

```bash
npm install
npm run dev